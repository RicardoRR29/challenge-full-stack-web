{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "test": "cross-env NODE_ENV=test dotenv -e .env.test -- prisma generate && jest"
  },
  "dependencies": {
    "@prisma/client": "^6.11.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv-cli": "^8.0.0",
    "jest": "^30.0.3",
    "nodemon": "^2.0.15",
    "prisma": "^6.11.0",
    "supertest": "^7.1.1"
  }
}
